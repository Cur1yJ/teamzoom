#sports
  #render_sports
    .border_users
      .btn-add= link_to "New Sport", new_admin_sport_path, :class => "btn btn-primary clear span", :remote => true
      .clear
      #render_sports 
        = render "sports"
#sport_wrapper
